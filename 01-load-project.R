library(here)
library(ProjectTemplate)

# 1. Choose which farm you want to work with by changing the farm number below:
source(here::here("config", "farm1_config.R"))

# 2. Then load the project
load.project()